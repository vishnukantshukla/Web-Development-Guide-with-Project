Structure, elements, attributes, semantics, charset, emojis(charset), 
layout(structure-> with semantics), why css, tags, classes, 
selectors, pseudoclasses, categories(child, relation...), 
forms, form level classes, element level classes, gradients.

Diff between element and tags
Elements are subset of tags